import FormUserConfig from "../../components/FormUserConfig/FormUserConfig";
import { Container } from "@mui/material";

const UserConfig = () => {
  return (
    <Container maxWidth="lg">
      <FormUserConfig />
    </Container>
  );
};

export default UserConfig;
